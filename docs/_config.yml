title: PromptCrafter API
description: API backend for managing prompt libraries and tracking AI output
remote_theme: mmistakes/minimal-mistakes
minimal_mistakes_skin: default
search: true

# Enable sidebar nav
defaults:
  - scope:
      path: ""
    values:
      layout: single
      sidebar:
        nav: "main"

# Build sidebar nav structure
collections:
  tutorials:
    output: true
  reference:
    output: true

# Sidebar navigation (stored in _data/navigation.yml)
